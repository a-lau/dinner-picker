

const initialState= {
  tab: "first"
}


const FormReducer = (state=initialState, action) => {
  /* switch blocks
   * switch (action.type)
   * case actions.THING:
   *  return {
   *  ...state
   *  };
   */
  return state;
}

export default FormReducer;
